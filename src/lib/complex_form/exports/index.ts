import { createForm } from "..";

const {
    Provider,
    useData,
    useDataOperations,
    useInitialData,
} = createForm()

export {
    Provider,
    useData,
    useDataOperations,
    useInitialData,
}